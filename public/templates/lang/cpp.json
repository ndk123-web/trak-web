{
  "id": "lang/cpp",
  "name": "Modern C++ Systems & Performance Mastery Track",
  "version": "1.3.0",
  "description": "Comprehensive Modern C++ curriculum covering references, RAII, smart pointers, STL containers, lambdas, and templates.",
  "root": {
    "name": "cpp-workspace",
    "type": "directory",
    "children": [
      {
        "name": "00-setup-toolchain-and-first-program",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 00 — Setup, Toolchain & First Program\n\n## What is this?\nWelcome to C++! C++ is a high-performance, multi-paradigm systems programming language offering object-oriented programming, generic templates, low-level memory control, and zero-cost abstractions. In this introductory module, you will verify your C++ compiler toolchain and run your first compiled C++ binary.\n\n## Why does C++ have it?\n- C++ compiles directly to machine code using modern optimizing compilers (GCC, Clang, MSVC).\n- The standard input/output stream library `<iostream>` provides type-safe stream operations via `std::cout` and `std::cin` without brittle format specifiers.\n\n## Minimal Anatomy of a C++ Program\nExamine `main.cpp`:\n\n```cpp\n#include <iostream>\n\nint main() {\n    std::cout << \"Hello, World!\\n\";\n    return 0;\n}\n```\n\n## How to Compile and Run\nOpen your terminal in this directory and execute:\n\n### 1. Compile the program using G++\n```bash\ng++ -Wall -Wextra -std=c++17 main.cpp -o main.exe\n```\n\n### 2. Run the compiled executable\nOn Windows:\n```bash\n.\\main.exe\n```\nOn Linux / macOS:\n```bash\n./main\n```\n\n### 3. Verify compiler installation\n```bash\ng++ --version\n```\n\n## Common Mistakes\n1. Forgetting the `std::` namespace prefix when using `cout` or `endl`.\n2. Omitting `#include <iostream>`.\n"
          },
          {
            "name": "main.cpp",
            "type": "file",
            "content": "#include <iostream>\n\nint main() {\n    std::cout << \"Hello, World!\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n\nint main() {\n    std::cout << \"C++ toolchain and runtime verified successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "01-variables-references-and-const",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 01 — Variables, References & Const\n\n## What is this?\nC++ introduces true **references** (`Type&`) as transparent aliases to existing variables. `const` enforces immutability, while `constexpr` specifies compile-time evaluation.\n\n## Why does C++ have it?\n- References provide pass-by-reference semantics with clean syntax, avoiding pointer syntax (`*`, `&`) and null-pointer hazards.\n- Passing large objects by `const Type&` eliminates expensive copy overhead while guaranteeing the original object cannot be mutated.\n\n## Syntax and Core Mechanics\n```cpp\nint x = 10;\nint& ref = x; // ref is an alias to x\nref = 20;     // x is now 20\n\nconst int max_users = 100; // immutable\n```\n\n## What to Practice\nImplement in `exercise.cpp`:\n1. `void double_value(int& val)`: Doubles the value of `val` in place via reference.\n2. `void swap_values(int& a, int& b)`: Swaps `a` and `b` using references.\n3. `constexpr int compute_square(int n)`: Returns `n * n`.\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\nvoid double_value(int& val) {\n    val *= 2;\n}\n\nvoid swap_values(int& a, int& b) {\n    int temp = a;\n    a = b;\n    b = temp;\n}\n\nconstexpr int compute_square(int n) {\n    return n * n;\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n\nvoid increment(int& x) { x++; }\n\nint main() {\n    int count = 5;\n    increment(count);\n    std::cout << \"Count after increment: \" << count << \"\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "void double_value(int& val) {\n    // TODO: Double val in place\n}\n\nvoid swap_values(int& a, int& b) {\n    // TODO: Swap a and b using references\n}\n\nconstexpr int compute_square(int n) {\n    // TODO: Return square of n\n    return 0;\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include \"exercise.cpp\"\n\nint main() {\n    int num = 15;\n    double_value(num);\n    assert(num == 30);\n\n    int x = 5, y = 10;\n    swap_values(x, y);\n    assert(x == 10 && y == 5);\n\n    assert(compute_square(4) == 16);\n    assert(compute_square(-3) == 9);\n\n    std::cout << \"Module 01 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "02-control-flow-and-functions",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 02 — Control Flow & Functions\n\n## What is this?\nC++ supports function overloading (multiple functions sharing the same name with different parameter signatures) and default parameter values.\n\n## Why does C++ have it?\n- Function overloading allows creating uniform, intuitive APIs (e.g. `print(int)` and `print(double)`).\n- Default arguments simplify function calls by allowing callers to omit optional parameters.\n\n## Syntax and Core Mechanics\n```cpp\nint multiply(int a, int b = 2); // default b to 2\n\n// Overloaded functions\nint add(int a, int b);\ndouble add(double a, double b);\n```\n\n## What to Practice\nImplement in `exercise.cpp`:\n1. `int clamp(int val, int low, int high)`: Returns `low` if `val < low`, `high` if `val > high`, else `val`.\n2. Overloaded functions:\n   - `int calculate_area(int side)`: Computes square area (`side * side`).\n   - `int calculate_area(int width, int height)`: Computes rectangle area (`width * height`).\n3. `int power_with_default(int base, int exp = 2)`: Computes `base^exp`. Assume `exp >= 0`.\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\nint clamp(int val, int low, int high) {\n    if (val < low) return low;\n    if (val > high) return high;\n    return val;\n}\n\nint calculate_area(int side) {\n    return side * side;\n}\n\nint calculate_area(int width, int height) {\n    return width * height;\n}\n\nint power_with_default(int base, int exp = 2) {\n    int res = 1;\n    for (int i = 0; i < exp; i++) {\n        res *= base;\n    }\n    return res;\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n\nvoid greet(const char* name = \"Guest\") {\n    std::cout << \"Hello, \" << name << \"\\n\";\n}\n\nint main() {\n    greet();\n    greet(\"Alice\");\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "int clamp(int val, int low, int high) {\n    return 0;\n}\n\nint calculate_area(int side) {\n    return 0;\n}\n\nint calculate_area(int width, int height) {\n    return 0;\n}\n\nint power_with_default(int base, int exp = 2) {\n    return 0;\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include \"exercise.cpp\"\n\nint main() {\n    assert(clamp(5, 10, 20) == 10);\n    assert(clamp(25, 10, 20) == 20);\n    assert(clamp(15, 10, 20) == 15);\n\n    assert(calculate_area(5) == 25);\n    assert(calculate_area(4, 6) == 24);\n\n    assert(power_with_default(5) == 25);     // default exp = 2\n    assert(power_with_default(2, 3) == 8);\n\n    std::cout << \"Module 02 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "03-strings-and-string-view",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 03 — Strings & Operations\n\n## What is this?\n`std::string` manages heap-allocated dynamic text with automatic memory management, string concatenation (`+`), substring search, and bounds operations.\n\n## Why does C++ have it?\n- Eliminates manual `malloc`, buffer overflows, and null terminator bugs common in C string manipulation.\n- Passing strings by `const std::string&` prevents unnecessary deep copies while providing read-only inspection.\n\n## Syntax and Core Mechanics\n```cpp\n#include <string>\n\nstd::string s = \"Hello\";\ns += \", World!\";\nsize_t len = s.length();\n```\n\n## What to Practice\nImplement in `exercise.cpp`:\n1. `bool is_palindrome(const std::string& s)`:\n   - Returns `true` if characters read the same forwards and backwards, else `false`.\n2. `std::string repeat_string(const std::string& s, int times)`:\n   - Returns a new `std::string` containing `s` repeated `times` times. Returns empty string if `times <= 0`.\n3. `int count_vowels(const std::string& s)`:\n   - Counts vowels (`a, e, i, o, u`, case-insensitive).\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\n#include <string>\n#include <cctype>\n\nbool is_palindrome(const std::string& s) {\n    if (s.empty()) return true;\n    size_t left = 0;\n    size_t right = s.size() - 1;\n    while (left < right) {\n        if (s[left] != s[right]) return false;\n        left++;\n        right--;\n    }\n    return true;\n}\n\nstd::string repeat_string(const std::string& s, int times) {\n    if (times <= 0) return \"\";\n    std::string result;\n    result.reserve(s.size() * times);\n    for (int i = 0; i < times; i++) {\n        result += s;\n    }\n    return result;\n}\n\nint count_vowels(const std::string& s) {\n    int count = 0;\n    for (char ch : s) {\n        char lower = std::tolower(static_cast<unsigned char>(ch));\n        if (lower == 'a' || lower == 'e' || lower == 'i' || lower == 'o' || lower == 'u') {\n            count++;\n        }\n    }\n    return count;\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <string>\n\nvoid inspect(const std::string& s) {\n    std::cout << \"Viewing: \" << s << \" (len: \" << s.size() << \")\\n\";\n}\n\nint main() {\n    std::string text = \"Modern C++\";\n    inspect(text);\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "#include <string>\n\nbool is_palindrome(const std::string& s) {\n    return false;\n}\n\nstd::string repeat_string(const std::string& s, int times) {\n    return \"\";\n}\n\nint count_vowels(const std::string& s) {\n    return 0;\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <string>\n#include \"exercise.cpp\"\n\nint main() {\n    assert(is_palindrome(\"racecar\") == true);\n    assert(is_palindrome(\"hello\") == false);\n    assert(is_palindrome(\"\") == true);\n\n    assert(repeat_string(\"abc\", 3) == \"abcabcabc\");\n    assert(repeat_string(\"xyz\", 0) == \"\");\n\n    assert(count_vowels(\"C++ Programming\") == 3);\n    assert(count_vowels(\"AEIOU\") == 5);\n\n    std::cout << \"Module 03 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "04-stl-vectors-and-iteration",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 04 — STL Vectors & Iteration\n\n## What is this?\n`std::vector<T>` is the default sequence container in C++. It encapsulates a dynamically resizing contiguous array on the heap.\n\n## Why does C++ have it?\n- Guarantees O(1) random access via `operator[]` and amortized O(1) appending via `push_back`.\n- Integrates with modern C++ range-based `for` loops (`for (const auto& item : vec)`).\n\n## Syntax and Core Mechanics\n```cpp\n#include <vector>\n\nstd::vector<int> nums = {1, 2, 3};\nnums.push_back(4);\nfor (int n : nums) { /* ... */ }\n```\n\n## What to Practice\nImplement in `exercise.cpp`:\n1. `int sum_vector(const std::vector<int>& vec)`: Returns sum of all elements.\n2. `std::vector<int> filter_even(const std::vector<int>& vec)`:\n   - Returns a new vector containing only the even integers in `vec`.\n3. `std::vector<int> reverse_vector(const std::vector<int>& vec)`:\n   - Returns a new vector with elements in reverse order.\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\n#include <vector>\n\nint sum_vector(const std::vector<int>& vec) {\n    int total = 0;\n    for (int n : vec) {\n        total += n;\n    }\n    return total;\n}\n\nstd::vector<int> filter_even(const std::vector<int>& vec) {\n    std::vector<int> result;\n    for (int n : vec) {\n        if (n % 2 == 0) {\n            result.push_back(n);\n        }\n    }\n    return result;\n}\n\nstd::vector<int> reverse_vector(const std::vector<int>& vec) {\n    std::vector<int> result;\n    result.reserve(vec.size());\n    for (int i = static_cast<int>(vec.size()) - 1; i >= 0; i--) {\n        result.push_back(vec[i]);\n    }\n    return result;\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<std::string> fruits = {\"Apple\", \"Banana\"};\n    fruits.push_back(\"Cherry\");\n\n    for (const auto& fruit : fruits) {\n        std::cout << fruit << \" \";\n    }\n    std::cout << \"\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "#include <vector>\n\nint sum_vector(const std::vector<int>& vec) {\n    return 0;\n}\n\nstd::vector<int> filter_even(const std::vector<int>& vec) {\n    return {};\n}\n\nstd::vector<int> reverse_vector(const std::vector<int>& vec) {\n    return {};\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <vector>\n#include \"exercise.cpp\"\n\nint main() {\n    std::vector<int> numbers = {1, 2, 3, 4, 5};\n    assert(sum_vector(numbers) == 15);\n\n    auto evens = filter_even(numbers);\n    assert(evens.size() == 2 && evens[0] == 2 && evens[1] == 4);\n\n    auto rev = reverse_vector(numbers);\n    assert(rev.size() == 5 && rev[0] == 5 && rev[4] == 1);\n\n    std::cout << \"Module 04 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "05-classes-and-encapsulation",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 05 — Classes & Encapsulation\n\n## What is this?\nClasses encapsulate member state (`private`) and public interface methods (`public`). In C++, members are `private` by default in a `class`, and `public` by default in a `struct`.\n\n## Why does C++ have it?\n- Enforces invariant validation and prevents outside code from putting objects into corrupted states.\n- Member functions marked `const` guarantee they will not mutate object state.\n\n## Syntax and Core Mechanics\n```cpp\nclass BankAccount {\nprivate:\n    double balance_ = 0.0;\npublic:\n    BankAccount(double initial) : balance_(initial) {}\n    double get_balance() const { return balance_; }\n};\n```\n\n## What to Practice\nIn `exercise.cpp`, implement class `Rectangle`:\n- Constructor: `Rectangle(double width, double height)` (stores width and height, defaults to 0 if negative).\n- `double get_width() const`\n- `double get_height() const`\n- `double get_area() const`\n- `double get_perimeter() const`\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\nclass Rectangle {\nprivate:\n    double width_ = 0.0;\n    double height_ = 0.0;\npublic:\n    Rectangle(double width, double height)\n        : width_(width > 0 ? width : 0.0),\n          height_(height > 0 ? height : 0.0) {}\n\n    double get_width() const { return width_; }\n    double get_height() const { return height_; }\n    double get_area() const { return width_ * height_; }\n    double get_perimeter() const { return 2 * (width_ + height_); }\n};\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n\nclass Counter {\nprivate:\n    int count_ = 0;\npublic:\n    void increment() { count_++; }\n    int get() const { return count_; }\n};\n\nint main() {\n    Counter c;\n    c.increment();\n    std::cout << \"Count: \" << c.get() << \"\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "class Rectangle {\npublic:\n    Rectangle(double width, double height) {}\n    double get_width() const { return 0.0; }\n    double get_height() const { return 0.0; }\n    double get_area() const { return 0.0; }\n    double get_perimeter() const { return 0.0; }\n};\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <cmath>\n#include \"exercise.cpp\"\n\nint main() {\n    Rectangle r(4.0, 5.0);\n    assert(r.get_width() == 4.0);\n    assert(r.get_height() == 5.0);\n    assert(r.get_area() == 20.0);\n    assert(r.get_perimeter() == 18.0);\n\n    Rectangle r_neg(-5.0, 10.0);\n    assert(r_neg.get_width() == 0.0);\n\n    std::cout << \"Module 05 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "06-destructors-and-raii",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 06 — Destructors & RAII\n\n## What is this?\nResource Acquisition Is Initialization (RAII) ties resource lifecycle to object lifetime. The constructor acquires resources, and the destructor (`~ClassName()`) automatically releases them when the object goes out of scope.\n\n## Why does C++ have it?\n- Eliminates resource leaks (memory, sockets, file descriptors, mutex locks) even when exceptions are thrown.\n\n## Syntax and Core Mechanics\n```cpp\nclass FileGuard {\n    FILE* file_;\npublic:\n    FileGuard(FILE* f) : file_(f) {}\n    ~FileGuard() { if (file_) fclose(file_); }\n};\n```\n\n## What to Practice\nIn `exercise.cpp`, implement class `BufferGuard`:\n- Allocates an array of `size` integers dynamically in heap memory (`new int[size]`).\n- Stores pointer `data_` and `size_`.\n- Destructor `~BufferGuard()` deallocates the array via `delete[] data_`.\n- Methods:\n  - `int get_size() const`\n  - `void set(int index, int val)`\n  - `int get(int index) const` (returns -1 if out of bounds)\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\nclass BufferGuard {\nprivate:\n    int* data_ = nullptr;\n    int size_ = 0;\npublic:\n    explicit BufferGuard(int size) : size_(size > 0 ? size : 0) {\n        if (size_ > 0) {\n            data_ = new int[size_]();\n        }\n    }\n\n    ~BufferGuard() {\n        delete[] data_;\n        data_ = nullptr;\n    }\n\n    // Disable copying for RAII ownership simplicity\n    BufferGuard(const BufferGuard&) = delete;\n    BufferGuard& operator=(const BufferGuard&) = delete;\n\n    int get_size() const { return size_; }\n\n    void set(int index, int val) {\n        if (index >= 0 && index < size_ && data_) {\n            data_[index] = val;\n        }\n    }\n\n    int get(int index) const {\n        if (index >= 0 && index < size_ && data_) {\n            return data_[index];\n        }\n        return -1;\n    }\n};\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n\nstruct Tracker {\n    Tracker() { std::cout << \"Acquired\\n\"; }\n    ~Tracker() { std::cout << \"Released\\n\"; }\n};\n\nint main() {\n    { Tracker t; } // Automatically destroyed at end of scope\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "class BufferGuard {\npublic:\n    explicit BufferGuard(int size) {}\n    ~BufferGuard() {}\n    BufferGuard(const BufferGuard&) = delete;\n    BufferGuard& operator=(const BufferGuard&) = delete;\n\n    int get_size() const { return 0; }\n    void set(int index, int val) {}\n    int get(int index) const { return -1; }\n};\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include \"exercise.cpp\"\n\nint main() {\n    {\n        BufferGuard buf(5);\n        assert(buf.get_size() == 5);\n        buf.set(0, 100);\n        buf.set(1, 200);\n        assert(buf.get(0) == 100);\n        assert(buf.get(1) == 200);\n        assert(buf.get(10) == -1); // out of bounds\n    } // destructor deletes buffer cleanly\n\n    std::cout << \"Module 06 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "07-operator-overloading",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 07 — Operator Overloading\n\n## What is this?\nOperator overloading allows custom classes to define behavior for standard operators like `+`, `==`, and `!=`.\n\n## What to Practice\nIn `exercise.cpp`, implement class `Vector2D`:\n- Fields: `int x`, `int y`.\n- Constructor `Vector2D(int x = 0, int y = 0)`.\n- Overload `Vector2D operator+(const Vector2D& other) const`\n- Overload `bool operator==(const Vector2D& other) const`\n- Overload `bool operator!=(const Vector2D& other) const`\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\nclass Vector2D {\npublic:\n    int x;\n    int y;\n\n    Vector2D(int x_val = 0, int y_val = 0) : x(x_val), y(y_val) {}\n\n    Vector2D operator+(const Vector2D& other) const {\n        return Vector2D(x + other.x, y + other.y);\n    }\n\n    bool operator==(const Vector2D& other) const {\n        return x == other.x && y == other.y;\n    }\n\n    bool operator!=(const Vector2D& other) const {\n        return !(*this == other);\n    }\n};\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n\nstruct Complex {\n    double real, imag;\n    Complex operator+(const Complex& o) const { return {real + o.real, imag + o.imag}; }\n};\n\nint main() {\n    Complex c1{1.0, 2.0}, c2{3.0, 4.0};\n    Complex c3 = c1 + c2;\n    std::cout << \"Result: \" << c3.real << \" + \" << c3.imag << \"i\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "class Vector2D {\npublic:\n    int x = 0;\n    int y = 0;\n\n    Vector2D(int x_val = 0, int y_val = 0) : x(x_val), y(y_val) {}\n\n    Vector2D operator+(const Vector2D& other) const {\n        return Vector2D();\n    }\n\n    bool operator==(const Vector2D& other) const {\n        return false;\n    }\n\n    bool operator!=(const Vector2D& other) const {\n        return false;\n    }\n};\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include \"exercise.cpp\"\n\nint main() {\n    Vector2D a(2, 3);\n    Vector2D b(4, 5);\n    Vector2D c = a + b;\n\n    assert(c.x == 6 && c.y == 8);\n    assert(a == Vector2D(2, 3));\n    assert(a != b);\n\n    std::cout << \"Module 07 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "08-inheritance-and-virtual-functions",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 08 — Inheritance & Virtual Functions\n\n## What is this?\nDynamic polymorphism in C++ is achieved through inheritance and `virtual` member functions dispatched via virtual method tables (`vtable`).\n\n## Syntax and Core Mechanics\n```cpp\nclass Shape {\npublic:\n    virtual ~Shape() = default;\n    virtual double area() const = 0; // Pure virtual interface\n};\n```\n\n## What to Practice\nIn `exercise.cpp`:\n1. Base class `Shape`:\n   - Virtual destructor `virtual ~Shape() = default;`\n   - Pure virtual method `virtual double area() const = 0;`\n2. Derived class `Circle : public Shape`:\n   - Constructor `Circle(double radius)`\n   - Override `double area() const override` (`3.14159 * radius * radius`)\n3. Derived class `Square : public Shape`:\n   - Constructor `Square(double side)`\n   - Override `double area() const override` (`side * side`)\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\nclass Shape {\npublic:\n    virtual ~Shape() = default;\n    virtual double area() const = 0;\n};\n\nclass Circle : public Shape {\nprivate:\n    double radius_;\npublic:\n    explicit Circle(double radius) : radius_(radius) {}\n    double area() const override {\n        return 3.14159 * radius_ * radius_;\n    }\n};\n\nclass Square : public Shape {\nprivate:\n    double side_;\npublic:\n    explicit Square(double side) : side_(side) {}\n    double area() const override {\n        return side_ * side_;\n    }\n};\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n\nclass Animal {\npublic:\n    virtual ~Animal() = default;\n    virtual void speak() const { std::cout << \"Animal sound\\n\"; }\n};\n\nclass Dog : public Animal {\npublic:\n    void speak() const override { std::cout << \"Woof!\\n\"; }\n};\n\nint main() {\n    Dog d;\n    Animal* ptr = &d;\n    ptr->speak();\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "class Shape {\npublic:\n    virtual ~Shape() = default;\n    virtual double area() const = 0;\n};\n\nclass Circle : public Shape {\npublic:\n    explicit Circle(double radius) {}\n    double area() const override { return 0.0; }\n};\n\nclass Square : public Shape {\npublic:\n    explicit Square(double side) {}\n    double area() const override { return 0.0; }\n};\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <cmath>\n#include \"exercise.cpp\"\n\nint main() {\n    Circle c(2.0);\n    Square s(3.0);\n\n    Shape* p1 = &c;\n    Shape* p2 = &s;\n\n    assert(std::fabs(p1->area() - (3.14159 * 4.0)) < 0.001);\n    assert(std::fabs(p2->area() - 9.0) < 0.001);\n\n    std::cout << \"Module 08 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "09-smart-pointers-unique-ptr",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 09 — Smart Pointers (unique_ptr)\n\n## What is this?\n`std::unique_ptr<T>` (defined in `<memory>`) manages heap objects with exclusive ownership. It cannot be copied, only moved. When the `unique_ptr` goes out of scope, it automatically destroys the managed object.\n\n## Syntax and Core Mechanics\n```cpp\n#include <memory>\n\nauto ptr = std::make_unique<int>(42);\n// Ownership transfer\nauto other = std::move(ptr); // ptr is now nullptr\n```\n\n## What to Practice\nIn `exercise.cpp`:\nGiven:\n```cpp\nstruct Task {\n    int id;\n    std::string title;\n};\n```\nImplement:\n1. `std::unique_ptr<Task> create_task(int id, std::string title)`:\n   - Allocates and returns a `std::unique_ptr<Task>`.\n2. `int get_task_id(const std::unique_ptr<Task>& task)`:\n   - Returns task id, or -1 if `task == nullptr`.\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\n#include <memory>\n#include <string>\n\nstruct Task {\n    int id;\n    std::string title;\n};\n\nstd::unique_ptr<Task> create_task(int id, std::string title) {\n    return std::make_unique<Task>(Task{id, std::move(title)});\n}\n\nint get_task_id(const std::unique_ptr<Task>& task) {\n    if (!task) return -1;\n    return task->id;\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <memory>\n\nint main() {\n    std::unique_ptr<int> val = std::make_unique<int>(100);\n    std::cout << \"Val: \" << *val << \"\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "#include <memory>\n#include <string>\n\nstruct Task {\n    int id;\n    std::string title;\n};\n\nstd::unique_ptr<Task> create_task(int id, std::string title) {\n    return nullptr;\n}\n\nint get_task_id(const std::unique_ptr<Task>& task) {\n    return -1;\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <memory>\n#include <string>\n#include \"exercise.cpp\"\n\nint main() {\n    auto t = create_task(101, \"Deploy Service\");\n    assert(t != nullptr);\n    assert(t->id == 101);\n    assert(t->title == \"Deploy Service\");\n    assert(get_task_id(t) == 101);\n\n    std::unique_ptr<Task> empty_task = nullptr;\n    assert(get_task_id(empty_task) == -1);\n\n    std::cout << \"Module 09 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "10-smart-pointers-shared-and-weak",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 10 — Smart Pointers (shared_ptr & weak_ptr)\n\n## What is this?\n`std::shared_ptr<T>` maintains shared ownership via an atomic reference count. `std::weak_ptr<T>` provides a non-owning observer to break circular reference cycles.\n\n## Syntax and Core Mechanics\n```cpp\n#include <memory>\n\nauto s1 = std::make_shared<int>(50);\nauto s2 = s1; // Ref count is now 2\nlong count = s1.use_count(); // 2\n```\n\n## What to Practice\nImplement in `exercise.cpp`:\n1. `std::shared_ptr<int> make_shared_int(int val)`: Allocates and returns a shared pointer to `val`.\n2. `long get_ref_count(const std::shared_ptr<int>& ptr)`: Returns `ptr.use_count()`.\n3. `bool is_expired(const std::weak_ptr<int>& w_ptr)`: Returns `w_ptr.expired()`.\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\n#include <memory>\n\nstd::shared_ptr<int> make_shared_int(int val) {\n    return std::make_shared<int>(val);\n}\n\nlong get_ref_count(const std::shared_ptr<int>& ptr) {\n    return ptr.use_count();\n}\n\nbool is_expired(const std::weak_ptr<int>& w_ptr) {\n    return w_ptr.expired();\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <memory>\n\nint main() {\n    std::shared_ptr<int> p1 = std::make_shared<int>(42);\n    std::weak_ptr<int> wp = p1;\n    std::cout << \"Count: \" << p1.use_count() << \", Expired: \" << wp.expired() << \"\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "#include <memory>\n\nstd::shared_ptr<int> make_shared_int(int val) {\n    return nullptr;\n}\n\nlong get_ref_count(const std::shared_ptr<int>& ptr) {\n    return 0;\n}\n\nbool is_expired(const std::weak_ptr<int>& w_ptr) {\n    return true;\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <memory>\n#include \"exercise.cpp\"\n\nint main() {\n    auto p1 = make_shared_int(42);\n    assert(p1 != nullptr && *p1 == 42);\n    assert(get_ref_count(p1) == 1);\n\n    auto p2 = p1;\n    assert(get_ref_count(p1) == 2);\n\n    std::weak_ptr<int> wp = p1;\n    assert(!is_expired(wp));\n\n    p1.reset();\n    p2.reset();\n    assert(is_expired(wp));\n\n    std::cout << \"Module 10 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "11-move-semantics-and-rvalues",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 11 — Move Semantics & Rvalues\n\n## What is this?\nMove semantics transfer internal resources (pointers, buffers) from temporary rvalue objects (`Type&&`) without performing expensive deep memory allocations. `std::move` casts an lvalue to an rvalue.\n\n## Syntax and Core Mechanics\n```cpp\nstd::string a = \"Large data\";\nstd::string b = std::move(a); // a is now empty\n```\n\n## What to Practice\nIn `exercise.cpp`, implement class `StringWrapper`:\n- Holds `std::string data_`.\n- Constructor: `StringWrapper(std::string s) : data_(std::move(s)) {}`\n- Move constructor: `StringWrapper(StringWrapper&& other) noexcept : data_(std::move(other.data_)) {}`\n- Method: `const std::string& get() const { return data_; }`\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\n#include <string>\n#include <utility>\n\nclass StringWrapper {\nprivate:\n    std::string data_;\npublic:\n    explicit StringWrapper(std::string s) : data_(std::move(s)) {}\n    StringWrapper(StringWrapper&& other) noexcept : data_(std::move(other.data_)) {}\n    StringWrapper& operator=(StringWrapper&& other) noexcept {\n        if (this != &other) {\n            data_ = std::move(other.data_);\n        }\n        return *this;\n    }\n    const std::string& get() const { return data_; }\n};\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <string>\n#include <utility>\n\nint main() {\n    std::string s1 = \"Alpha\";\n    std::string s2 = std::move(s1);\n    std::cout << \"s2: \" << s2 << \", s1 empty? \" << s1.empty() << \"\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "#include <string>\n\nclass StringWrapper {\nprivate:\n    std::string data_;\npublic:\n    explicit StringWrapper(std::string s) {}\n    StringWrapper(StringWrapper&& other) noexcept {}\n    const std::string& get() const { return data_; }\n};\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <string>\n#include \"exercise.cpp\"\n\nint main() {\n    StringWrapper w1(\"Persistent Resource\");\n    assert(w1.get() == \"Persistent Resource\");\n\n    StringWrapper w2 = std::move(w1);\n    assert(w2.get() == \"Persistent Resource\");\n\n    std::cout << \"Module 11 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "12-function-templates",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 12 — Function Templates\n\n## What is this?\nTemplates allow writing type-agnostic code parameterized by types at compile time.\n\n## Syntax and Core Mechanics\n```cpp\ntemplate <typename T>\nT max_val(T a, T b) { return (a > b) ? a : b; }\n```\n\n## What to Practice\nImplement in `exercise.cpp`:\n1. `template <typename T> T find_min(T a, T b)`: Returns smaller of `a` and `b`.\n2. `template <typename T> void swap_generic(T& a, T& b)`: Swaps two items of type `T`.\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\ntemplate <typename T>\nT find_min(T a, T b) {\n    return (a < b) ? a : b;\n}\n\ntemplate <typename T>\nvoid swap_generic(T& a, T& b) {\n    T temp = a;\n    a = b;\n    b = temp;\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n\ntemplate <typename T>\nvoid display(T val) { std::cout << \"Val: \" << val << \"\\n\"; }\n\nint main() {\n    display(42);\n    display(\"Template string\");\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "template <typename T>\nT find_min(T a, T b) {\n    return a;\n}\n\ntemplate <typename T>\nvoid swap_generic(T& a, T& b) {\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <string>\n#include \"exercise.cpp\"\n\nint main() {\n    assert(find_min(10, 20) == 10);\n    assert(find_min(3.14, 2.71) == 2.71);\n\n    int a = 1, b = 2;\n    swap_generic(a, b);\n    assert(a == 2 && b == 1);\n\n    std::string s1 = \"first\", s2 = \"second\";\n    swap_generic(s1, s2);\n    assert(s1 == \"second\" && s2 == \"first\");\n\n    std::cout << \"Module 12 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "13-class-templates",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 13 — Class Templates\n\n## What is this?\nClass templates define generic classes capable of operating over arbitrary types.\n\n## What to Practice\nIn `exercise.cpp`, implement class template `Box<T>`:\n- Private member `T value_`.\n- Constructor `Box(T val) : value_(val) {}`\n- Method `T get() const`\n- Method `void set(T val)`\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\ntemplate <typename T>\nclass Box {\nprivate:\n    T value_;\npublic:\n    explicit Box(T val) : value_(val) {}\n    T get() const { return value_; }\n    void set(T val) { value_ = val; }\n};\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n\ntemplate <typename T>\nstruct Pair { T first, second; };\n\nint main() {\n    Pair<int> p{1, 2};\n    std::cout << p.first << \", \" << p.second << \"\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "template <typename T>\nclass Box {\npublic:\n    explicit Box(T val) {}\n    T get() const { return T(); }\n    void set(T val) {}\n};\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <string>\n#include \"exercise.cpp\"\n\nint main() {\n    Box<int> int_box(42);\n    assert(int_box.get() == 42);\n    int_box.set(100);\n    assert(int_box.get() == 100);\n\n    Box<std::string> str_box(\"Hello\");\n    assert(str_box.get() == \"Hello\");\n\n    std::cout << \"Module 13 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "14-stl-associative-containers",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 14 — STL Associative Containers\n\n## What is this?\nAssociative containers include `std::map` (ordered Red-Black tree, O(log N)) and `std::unordered_map` (hash table, average O(1)).\n\n## What to Practice\nImplement in `exercise.cpp`:\n1. `void record_word(std::unordered_map<std::string, int>& counts, const std::string& word)`:\n   - Increments the frequency count of `word` in `counts`.\n2. `int get_word_count(const std::unordered_map<std::string, int>& counts, const std::string& word)`:\n   - Returns count of `word`, or 0 if not present.\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\n#include <string>\n#include <unordered_map>\n\nvoid record_word(std::unordered_map<std::string, int>& counts, const std::string& word) {\n    counts[word]++;\n}\n\nint get_word_count(const std::unordered_map<std::string, int>& counts, const std::string& word) {\n    auto it = counts.find(word);\n    if (it != counts.end()) {\n        return it->second;\n    }\n    return 0;\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <unordered_map>\n\nint main() {\n    std::unordered_map<std::string, int> ages;\n    ages[\"Alice\"] = 30;\n    std::cout << \"Alice: \" << ages[\"Alice\"] << \"\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "#include <string>\n#include <unordered_map>\n\nvoid record_word(std::unordered_map<std::string, int>& counts, const std::string& word) {\n}\n\nint get_word_count(const std::unordered_map<std::string, int>& counts, const std::string& word) {\n    return 0;\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <string>\n#include <unordered_map>\n#include \"exercise.cpp\"\n\nint main() {\n    std::unordered_map<std::string, int> map;\n    record_word(map, \"apple\");\n    record_word(map, \"banana\");\n    record_word(map, \"apple\");\n\n    assert(get_word_count(map, \"apple\") == 2);\n    assert(get_word_count(map, \"banana\") == 1);\n    assert(get_word_count(map, \"cherry\") == 0);\n\n    std::cout << \"Module 14 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "15-stl-algorithms-and-iterators",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 15 — STL Algorithms & Iterators\n\n## What is this?\nThe `<algorithm>` library provides standardized algorithms like `std::sort`, `std::count_if`, and `std::transform`.\n\n## What to Practice\nImplement in `exercise.cpp`:\n1. `void sort_descending(std::vector<int>& vec)`: Sorts vector elements in descending order.\n2. `int count_greater_than(const std::vector<int>& vec, int threshold)`:\n   - Counts elements strictly greater than `threshold`.\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\n#include <vector>\n#include <algorithm>\n\nvoid sort_descending(std::vector<int>& vec) {\n    std::sort(vec.begin(), vec.end(), [](int a, int b) { return a > b; });\n}\n\nint count_greater_than(const std::vector<int>& vec, int threshold) {\n    return static_cast<int>(std::count_if(vec.begin(), vec.end(), [threshold](int n) {\n        return n > threshold;\n    }));\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <vector>\n#include <algorithm>\n\nint main() {\n    std::vector<int> v = {3, 1, 4};\n    std::sort(v.begin(), v.end());\n    std::cout << \"First: \" << v[0] << \"\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "#include <vector>\n\nvoid sort_descending(std::vector<int>& vec) {\n}\n\nint count_greater_than(const std::vector<int>& vec, int threshold) {\n    return 0;\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <vector>\n#include \"exercise.cpp\"\n\nint main() {\n    std::vector<int> nums = {4, 1, 9, 2};\n    sort_descending(nums);\n    assert(nums[0] == 9 && nums[1] == 4 && nums[2] == 2 && nums[3] == 1);\n\n    assert(count_greater_than(nums, 3) == 2); // 9 and 4\n\n    std::cout << \"Module 15 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "16-lambdas-and-std-function",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 16 — Lambdas & std::function\n\n## What is this?\nC++ lambdas define anonymous closures: `[capture](params) -> return_type { body }`. `std::function` wraps any callable.\n\n## What to Practice\nImplement in `exercise.cpp`:\n1. `std::function<int(int)> make_multiplier(int factor)`:\n   - Returns a lambda that multiplies its argument by `factor`.\n2. `std::vector<int> transform_values(const std::vector<int>& vec, const std::function<int(int)>& op)`:\n   - Applies `op` to each element and returns new vector.\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\n#include <functional>\n#include <vector>\n\nstd::function<int(int)> make_multiplier(int factor) {\n    return [factor](int x) { return x * factor; };\n}\n\nstd::vector<int> transform_values(const std::vector<int>& vec, const std::function<int(int)>& op) {\n    std::vector<int> result;\n    result.reserve(vec.size());\n    for (int n : vec) {\n        result.push_back(op(n));\n    }\n    return result;\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n\nint main() {\n    auto sq = [](int x) { return x * x; };\n    std::cout << \"5 sq: \" << sq(5) << \"\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "#include <functional>\n#include <vector>\n\nstd::function<int(int)> make_multiplier(int factor) {\n    return nullptr;\n}\n\nstd::vector<int> transform_values(const std::vector<int>& vec, const std::function<int(int)>& op) {\n    return {};\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <vector>\n#include <functional>\n#include \"exercise.cpp\"\n\nint main() {\n    auto triple = make_multiplier(3);\n    assert(triple(5) == 15);\n\n    std::vector<int> input = {1, 2, 3};\n    auto out = transform_values(input, triple);\n    assert(out.size() == 3 && out[0] == 3 && out[1] == 6 && out[2] == 9);\n\n    std::cout << \"Module 16 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "17-error-handling-and-exceptions",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 17 — Error Handling & Exceptions\n\n## What is this?\nC++ provides structured exception handling via `try`, `catch`, and `throw`. Standard exceptions derive from `std::exception`.\n\n## What to Practice\nImplement in `exercise.cpp`:\n1. `int safe_divide_throws(int a, int b)`:\n   - If `b == 0`, throws `std::invalid_argument(\"Division by zero\")`. Otherwise returns `a / b`.\n2. `bool try_divide(int a, int b, int& out_result)`:\n   - Calls `safe_divide_throws`. If successful, sets `out_result` and returns `true`. If exception caught, returns `false`.\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\n#include <stdexcept>\n\nint safe_divide_throws(int a, int b) {\n    if (b == 0) {\n        throw std::invalid_argument(\"Division by zero\");\n    }\n    return a / b;\n}\n\nbool try_divide(int a, int b, int& out_result) {\n    try {\n        out_result = safe_divide_throws(a, b);\n        return true;\n    } catch (const std::invalid_argument&) {\n        return false;\n    }\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <stdexcept>\n\nint main() {\n    try {\n        throw std::runtime_error(\"Demo error\");\n    } catch (const std::exception& e) {\n        std::cout << \"Caught: \" << e.what() << \"\\n\";\n    }\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "int safe_divide_throws(int a, int b) {\n    return 0;\n}\n\nbool try_divide(int a, int b, int& out_result) {\n    return false;\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <stdexcept>\n#include \"exercise.cpp\"\n\nint main() {\n    int res = 0;\n    assert(try_divide(10, 2, res) == true);\n    assert(res == 5);\n\n    assert(try_divide(10, 0, res) == false);\n\n    bool threw = false;\n    try {\n        safe_divide_throws(5, 0);\n    } catch (const std::invalid_argument&) {\n        threw = true;\n    }\n    assert(threw);\n\n    std::cout << \"Module 17 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "18-file-io-and-streams",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 18 — File I/O & Streams\n\n## What is this?\n`<fstream>` provides RAII file stream classes (`std::ofstream`, `std::ifstream`).\n\n## What to Practice\nImplement in `exercise.cpp`:\n1. `bool write_text(const std::string& path, const std::string& content)`:\n   - Writes `content` to `path`. Returns `true` on success, `false` on failure.\n2. `std::string read_text(const std::string& path)`:\n   - Reads entire content of file at `path` into a string. Returns empty string if opening fails.\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\n#include <string>\n#include <fstream>\n#include <sstream>\n\nbool write_text(const std::string& path, const std::string& content) {\n    std::ofstream ofs(path);\n    if (!ofs.is_open()) return false;\n    ofs << content;\n    return ofs.good();\n}\n\nstd::string read_text(const std::string& path) {\n    std::ifstream ifs(path);\n    if (!ifs.is_open()) return \"\";\n    std::ostringstream ss;\n    ss << ifs.rdbuf();\n    return ss.str();\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <fstream>\n\nint main() {\n    std::ofstream out(\"demo.tmp\");\n    out << \"Testing\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "#include <string>\n\nbool write_text(const std::string& path, const std::string& content) {\n    return false;\n}\n\nstd::string read_text(const std::string& path) {\n    return \"\";\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <string>\n#include <cstdio>\n#include \"exercise.cpp\"\n\nint main() {\n    const std::string tmp = \"temp_test_cpp.txt\";\n    assert(write_text(tmp, \"Hello Stream!\") == true);\n    assert(read_text(tmp) == \"Hello Stream!\");\n    std::remove(tmp.c_str());\n\n    assert(read_text(\"non_existent_file_xyz.txt\") == \"\");\n\n    std::cout << \"Module 18 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      },
      {
        "name": "19-pair-and-tuple-utilities",
        "type": "directory",
        "children": [
          {
            "name": "README.md",
            "type": "file",
            "content": "# 19 — Pair & Tuple Utilities\n\n## What is this?\nC++ provides `<utility>` (`std::pair`) and `<tuple>` (`std::tuple`) for grouping multiple heterogeneous values without needing to define a dedicated struct.\n\n## Why does C++ have it?\n- Allows functions to return multiple values cleanly without requiring out-parameters or pointers.\n- Elements in a tuple are accessed at compile time via `std::get<Index>(t)`.\n\n## Syntax and Core Mechanics\n```cpp\n#include <utility>\n#include <tuple>\n\nstd::pair<int, std::string> p = {1, \"One\"};\nstd::tuple<int, double, std::string> t = {1, 3.14, \"Alpha\"};\nint id = std::get<0>(t);\n```\n\n## What to Practice\nImplement in `exercise.cpp`:\n1. `std::pair<int, int> find_min_max(const std::vector<int>& vec)`:\n   - Returns `{min_element, max_element}`. If `vec` is empty, returns `{0, 0}`.\n2. `std::pair<std::string, int> create_student_record(std::string name, int score)`:\n   - Returns a pair containing `name` and `score`.\n\n## Reference Solution\n\n<details>\n<summary>Click to reveal reference solution</summary>\n\n```cpp\n#include <utility>\n#include <vector>\n#include <string>\n\nstd::pair<int, int> find_min_max(const std::vector<int>& vec) {\n    if (vec.empty()) return {0, 0};\n    int min_val = vec[0];\n    int max_val = vec[0];\n    for (size_t i = 1; i < vec.size(); i++) {\n        if (vec[i] < min_val) min_val = vec[i];\n        if (vec[i] > max_val) max_val = vec[i];\n    }\n    return {min_val, max_val};\n}\n\nstd::pair<std::string, int> create_student_record(std::string name, int score) {\n    return {std::move(name), score};\n}\n```\n</details>\n"
          },
          {
            "name": "example.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <utility>\n\nint main() {\n    std::pair<std::string, int> item = {\"Widget\", 42};\n    std::cout << item.first << \": \" << item.second << \"\\n\";\n    return 0;\n}\n"
          },
          {
            "name": "exercise.cpp",
            "type": "file",
            "content": "#include <utility>\n#include <vector>\n#include <string>\n\nstd::pair<int, int> find_min_max(const std::vector<int>& vec) {\n    return {0, 0};\n}\n\nstd::pair<std::string, int> create_student_record(std::string name, int score) {\n    return {\"\", 0};\n}\n"
          },
          {
            "name": "exercise_test.cpp",
            "type": "file",
            "content": "#include <iostream>\n#include <cstdlib>\n\ninline void test_assert(bool condition, const char *expr, int line) {\n    if (!condition) {\n        std::cerr << \"Assertion failed: \" << expr << \" at line \" << line << \"\\n\";\n        std::exit(1);\n    }\n}\n#define assert(expr) test_assert(static_cast<bool>(expr), #expr, __LINE__)\n\n#include <utility>\n#include <vector>\n#include <string>\n#include \"exercise.cpp\"\n\nint main() {\n    std::vector<int> nums = {5, 2, 9, 1, 7};\n    auto p = find_min_max(nums);\n    assert(p.first == 1 && p.second == 9);\n\n    std::vector<int> empty_vec;\n    auto empty_p = find_min_max(empty_vec);\n    assert(empty_p.first == 0 && empty_p.second == 0);\n\n    auto rec = create_student_record(\"Bob\", 95);\n    assert(rec.first == \"Bob\" && rec.second == 95);\n\n    std::cout << \"Module 19 tests passed successfully.\\n\";\n    return 0;\n}\n"
          }
        ]
      }
    ]
  }
}